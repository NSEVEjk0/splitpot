# Splitpot environment (names only — no values committed)

# Moove Receive Agent API
MOOVE_API_KEY=
MOOVE_API_BASE_URL=https://api.moove.xyz
MOOVE_HANDLE=@ckay

# Admin token that gates pot creation
ADMIN_TOKEN=

# Optional hosted SQLite (Turso). If unset, a local file DB is used.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
